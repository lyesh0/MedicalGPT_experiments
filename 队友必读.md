# 队友必读 —— 仓库内容说明

> **课程题目**: 面向医疗场景的大语言模型安全对齐方法对比研究  
> **仓库**: https://github.com/lyesh0/MedicalGPT_experiments  
> **分支**: `exp/sft-ablation`

---

## 一、仓库里有什么？

克隆下来后，下面这些是你真正需要的：

```
MedicalGPT_experiments/
│
├── 课程报告_医疗大模型安全对齐方法对比研究.md   ← ★ 最终报告（3.3万字）
│
├── outputs/experiments/
│   ├── reports/
│   │   ├── curves/                 ← ★ 11张训练曲线PNG图（做PPT用）
│   │   │   ├── sft_C.png           SFT-C 2B 训练曲线
│   │   │   ├── dpo_medical.png     DPO 2B 训练曲线
│   │   │   ├── rm_medical.png      RM 2B 训练曲线
│   │   │   ├── rloo_medical.png    RLOO 2B 训练曲线
│   │   │   ├── grpo_medical_safety.png       GRPO v1 2B
│   │   │   ├── grpo_medical_safety_v2.png    GRPO v2 2B
│   │   │   ├── sft_7b_C.png        SFT-C 7B 训练曲线
│   │   │   ├── dpo_7b_medical.png  DPO 7B 训练曲线
│   │   │   ├── rm_7b_medical.png   RM 7B 训练曲线
│   │   │   ├── rloo_7b_medical.png RLOO 7B 训练曲线
│   │   │   └── grpo_7b_medical_safety.png    GRPO 7B 训练曲线
│   │   ├── phase1_sft_ablation.md  SFT 消融实验报告（2B）
│   │   ├── phase3_dpo_medical.md   DPO 实验报告（2B）
│   │   ├── 7b_full_report.md       7B 完整实验结果
│   │   └── 7b_alignment_analysis.md 7B 三种对齐方法机理分析
│   │
│   ├── predictions/                ← 推理结果（JSONL，每种模型50条回答）
│   │   ├── sft_C_predictions.jsonl
│   │   ├── dpo_predictions.jsonl
│   │   ├── grpo_predictions.jsonl
│   │   ├── rloo_predictions.jsonl  等...
│   │
│   ├── predictions_7b/             ← 7B 推理结果
│   │
│   ├── scores/                     ← 评分结果（JSON/CSV）
│   │   ├── medical_comparison.json ← 报告中的表格数据来源
│   │   └── per_sample_scores.jsonl
│   │
│   └── scores_7b/                  ← 7B 评分结果
│       ├── medical_comparison.json
│       ├── llm_judge_results.jsonl ← LLM API 精评结果
│       └── per_sample_scores.jsonl
│
├── data/experiments/
│   ├── eval/medical_eval_50.jsonl          ← 固定评测集（50条）
│   ├── preference_medical_safety/train.jsonl ← DPO/RM 偏好数据（250对）
│   ├── grpo_medical_safety/train.jsonl    ← GRPO 训练数据
│   └── rloo_prompts/train.jsonl           ← RLOO 训练数据
│
├── scripts/experiments/           ← 所有训练/评测脚本
│   ├── run_sft_training.sh        SFT 消融训练
│   ├── run_dpo_medical.sh         DPO 训练
│   ├── run_rm_medical.sh          RM 训练
│   ├── run_rloo_medical.sh        RLOO 训练
│   ├── run_grpo_medical_safety.sh GRPO 训练
│   ├── run_medical_eval.sh        批量评测
│   └── run_*_7b.sh                7B 对应脚本
│
├── tools/experiments/             ← 工具脚本
│   ├── build_medical_preference_data.py ← 偏好数据构造
│   ├── score_medical_outputs.py         ← 规则评分引擎
│   ├── llm_judge.py                     ← LLM 精评
│   ├── batch_inference.py               ← 批量推理
│   └── score_reward_model.py            ← RM 评估
│
├── CLAUDE.md          ← 快速导航（conda环境、训练命令、关键文件路径）
├── PLAN.md            ← 完整实验方案（含执行状态）
├── LEARNING_LOG.md    ← 实验学习日志（教训总结）
└── .gitignore
```

## 二、仓库里没有的（需要自己跑）

| 缺失内容 | 原因 | 怎么办 |
|---------|------|--------|
| LoRA adapter 模型权重 | 太大（~6.5GB）无法上传 GitHub | 运行 `scripts/experiments/` 下的训练脚本重新生成 |
| merged SFT-7B-C | 太大（15GB） | 运行 `run_sft_training_7b.sh --group C` 生成 |
| SFT 原始训练数据 | 太大且可下载 | 运行 `run_sft_data_pipeline.sh` 自动下载和处理 |

具体训练命令见 `CLAUDE.md`。

## 三、做 PPT 需要的关键素材

### 关键数据表（从报告中截取）

1. **SFT 消融对比表**（报告 5.1.1 节）
   - A/B/C 三组的 Composite、Safety、Completeness、Validity
2. **DPO 效果表**（报告 5.1.2 节）
   - 急症 safety 0.13→0.47（+251%），用药 0.07→0.67（+900%）
3. **7B 三种方法对比——规则评分**（报告 5.2.3 节）
   - GRPO 0.831 > SFT 0.815 > DPO 0.810
4. **7B 三种方法对比——LLM Judge**（报告 5.2.4 节）
   - DPO 6.58 > GRPO 6.42 > SFT 6.38
5. **方法论对比表**（报告 5.3.1 节）
   - DPO vs GRPO vs RLOO：信号来源/类型/复杂度/效率
6. **三种方法"评分交叉"图**（报告 5.3.3 节）
   - GRPO 规则评分第一 / DPO LLM Judge 第一

### 关键图（从 curves/ 取）

- `sft_C.png` — SFT 消融训练曲线对比（或三张分别放）
- `dpo_medical.png` — DPO 训练 loss + rewards 曲线
- `grpo_medical_safety.png` — GRPO reward 上升曲线
- `sft_7b_C.png` + `dpo_7b_medical.png` + `grpo_7b_medical_safety.png` — 7B 三件套
- 建议：2B 和 7B 的对应方法并列展示（如 sft_C.png | sft_7b_C.png）

### 代码截图

- `tools/experiments/score_medical_outputs.py` 的安全关键词词典（行22-44）
- `tools/experiments/build_medical_preference_data.py` 的双 prompt 策略（行45-56）
- `scripts/experiments/run_sft_training.sh` 的消融设计（行34-36，三组数据配置）

## 四、如何验证报告中的数字

```bash
# 查看 2B 规则评分对比
python -c "
import json
with open('outputs/experiments/scores/medical_comparison.json') as f:
    data = json.load(f)
for r in data:
    o = r['overall']
    print(f\"{r['model']:10s} composite={o['composite']:.3f}  safety={o['avg_safety']:.3f}\")
"

# 查看 7B LLM Judge 评分
python -c "
import json
rows = []
with open('outputs/experiments/scores_7b/llm_judge_results.jsonl') as f:
    for line in f:
        rows.append(json.loads(line))
from collections import defaultdict
ms = defaultdict(lambda: defaultdict(list))
for r in rows:
    for k in ['safety','accuracy','completeness','actionability','risk_control','overall']:
        ms[r['model']][k].append(r[k])
for m in sorted(ms):
    avg = {k: sum(v)/len(v) for k,v in ms[m].items()}
    print(f\"{m}: overall={avg['overall']:.2f}  safety={avg['safety']:.2f}  accuracy={avg['accuracy']:.2f}\")
"
```

## 五、注意事项

- 所有实验在 `conda activate medicalgpt` 环境下运行
- 基座模型在 `/root/autodl-tmp/models/`（不在仓库里，太大了）
- API key 在 `.env` 文件里（不在仓库里，需要自己配）
- 仓库基于 [MedicalGPT](https://github.com/shibing624/MedicalGPT) 开源项目
- 如需重新生成 LLM Judge 评分，需要 DeepSeek API key
